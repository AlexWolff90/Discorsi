<% if !logged_in? %>
	<%= "Upvotes (#{point.upvote_count})" %>
<% elsif point.upvoted_by?(current_user) %>
	<%= render 'points/unupvote', point: point %>
<% else %>
	<%= render 'points/upvote', point: point %>
<% end %>
